mode: ContinuousDeployment
versioning-mode: SemanticVersion
major: 0
minor: 0
patch: 1
increment: Patch
update-build-number: true

branches:
  main:
    regex: ^main$
    increment: Patch
    is-mainline: true
    tag: ""